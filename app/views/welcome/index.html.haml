= render partial: "shared/base_navbar", locals: {title: "Home"} unless current_user.blank?
.jumbotron
  .container
    -#
      %h1.text-center{ :style => "color: #2563AB"}
    - unless current_user.blank?
      %h2.text-center
        Scrap Yard Dog
      .col-sm-12.col-md-4.col-md-offset-4
        %a.btn.btn-primary.btn-lg{:href => "#{current_user.images_table? ? images_path : shipments_path}", :role => "button", :class => 'btn-block', data: { disable_with: "Searching..." }} Today
    - else
      .col-sm-12.col-md-4.col-md-offset-4
        %a.btn.btn-primary.btn-lg{:href => new_user_session_path, :role => "button", :class => 'btn-block', data: { disable_with: "Please wait..." }} Log In

= render partial: "shared/footer_navbar", locals: {title: "Welcome"} unless current_user.blank?