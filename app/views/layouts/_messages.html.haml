-# flash.each do |name, msg|
- flash.each do |msg_type, msg|
  - if msg.is_a?(String)
    %div{class: "alert alert-#{msg_type == :notice ? "success" : "danger"}"}
      %button.close{"aria-hidden" => "true", "data-dismiss" => "alert", type: "button"} ×
      = content_tag :div, msg, :id => "flash_#{name}"